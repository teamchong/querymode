{
  "name": "edgeq-wasm",
  "version": "0.1.0",
  "private": true,
  "description": "Zig WASM engine for EdgeQ — column decoding, SIMD ops, SQL execution, vector search",
  "type": "module",
  "scripts": {
    "build": "zig build wasm && cp zig-out/bin/edgeq.wasm dist/",
    "build:js": "cp examples/wasm/edgeq.js dist/"
  },
  "author": "Steven Chong",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}
